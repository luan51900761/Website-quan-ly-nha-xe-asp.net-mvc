@using (Html.BeginForm("Detail", "KiGuiUser", FormMethod.Post,new { target="_blank" }))
{
        <div class="form-group">
            <H4 class="bookticket-title">Tra cứu hóa đơn</H4>
            <div class="row form-search-bill justify-content-between">
                <input class="form-control col-8" type="text" name="code" value="" placeholder="Nhập mã hóa đơn" />
                <button class="btn btn-success col-3" type="submit">Tra cứu</button>
            </div>
        </div>
    }

    <script type='text/javascript'>
        $(function () {
            // When your page loads, create a delay (5s) to hide any errored elements
            setTimeout(function () { $('.validationSumary').fadeOut(); }, 2000);
        });
    </script>
    <style>
        .sliders {
            display: none;
        }

        .validation-summary-valid {
            display: none;
        }
    </style>
